# AstrBot Kiang 插件

## 简介
AstrBot Kiang 是一个简单的语音消息插件，当用户发送特定命令时，会回复一段预设的音频文件。

## 功能特性
- 支持自定义触发命令
- 可配置音频文件路径
- 当收到指定命令时发送音频消息

## 配置说明
- `commands`: 触发命令列表，默认为 ["kiang"]
- `audio`: 音频文件路径，默认为插件数据目录下的 Kiang.wav

## 使用方法
在聊天中输入配置的命令（默认为 "kiang"），机器人将发送指定的音频文件作为回复。

## 版本信息
- 版本: v1.0.0
- 作者: czqwq